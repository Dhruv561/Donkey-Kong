/**
 * Interface which is used for game objects which can be collected by entities
 */
public interface Collectable {
    /**
     * Interface method which allows an entity to collect an object
     */
    void collect();
}
